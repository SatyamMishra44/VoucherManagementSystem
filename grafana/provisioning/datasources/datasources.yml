apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    uid: prometheus
    editable: true

  - name: db_prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: db_prometheus
    editable: true

  - name: Loki

    type: loki
    access: proxy
    url: http://loki:3100
    uid: loki
    editable: true

